# 1.4 工具/API 接口文档

## 1. HTTP API 总览

服务入口：FastAPI。

默认本地地址：

```text
http://127.0.0.1:8000
```

前端 Vite 代理：

```text
http://localhost:5173 -> http://127.0.0.1:8000
```

## 2. 基础接口

### 2.1 健康检查

```http
GET /health
```

响应：

```json
{"status": "ok"}
```

### 2.2 根路径

```http
GET /
```

用途：查看应用名称、版本、阶段和 Swagger 地址。

### 2.3 配置摘要

```http
GET /config
```

用途：查看当前环境、Chroma、模型、MCP、LangSmith 和 API Key 是否配置。

注意：接口只返回 key 是否设置，不返回密钥明文。

## 3. Chat API

### 3.1 非流式问答

```http
POST /api/chat
Content-Type: application/json
```

请求：

```json
{
  "question": "什么是 ITPM 策略？",
  "session_id": "可选，会话 ID",
  "user_id": "default"
}
```

响应：

```json
{
  "answer": "回答正文",
  "citations": ["4.2.2"],
  "score": null,
  "session_id": "服务端会话 ID"
}
```

行为：

- 如果没有传 `session_id`，后端创建新会话。
- 如果传入已有 `session_id`，后端加载最近 N 轮消息作为短期上下文。
- 本轮问答结束后写入 SQLite 会话库。

### 3.2 SSE 流式问答

```http
POST /api/chat/stream
Content-Type: application/json
Accept: text/event-stream
```

请求：

```json
{
  "question": "统计各专业的事务数量",
  "session_id": "可选，会话 ID",
  "user_id": "default"
}
```

SSE 事件：

| 事件 | 数据字段 | 含义 |
| --- | --- | --- |
| `token` | `text` | LLM 输出文本 chunk |
| `tool_call` | `name`, `args` | LLM 或 Supervisor 决定调用工具 |
| `tool_result` | `name`, `preview` | 工具执行结果预览 |
| `meta` | `citations`, `session_id`, `short_term_count`, `chart_data_uri` 等 | 尾部元数据 |
| `done` | `{}` | 流结束 |
| `error` | `message` | 异常信息 |

前端注意：

- 浏览器原生 `EventSource` 不支持 POST body，所以前端使用 `fetch` 读取 stream。
- SQL 分支可能不产生 token，前端会 fallback 到 `/api/chat` 获取完整答案。

## 4. Session API

### 4.1 会话列表

```http
GET /api/sessions?user_id=default&limit=30
```

响应字段：

| 字段 | 含义 |
| --- | --- |
| `id` | 会话 ID |
| `user_id` | 用户 ID |
| `title` | 会话标题 |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |
| `message_count` | 消息数 |

### 4.2 会话详情

```http
GET /api/sessions/{session_id}?user_id=default
```

响应：

```json
{
  "session": {},
  "messages": []
}
```

### 4.3 删除会话

```http
DELETE /api/sessions/{session_id}?user_id=default
```

如果传入 `user_id`，后端会先校验该会话属于当前用户，再执行删除；不属于该用户时返回 404。

响应：

```json
{"deleted": true}
```

## 5. LangChain 工具

| 工具 | 文件 | 用途 |
| --- | --- | --- |
| `convert_unit` | `tools/units.py` | 压力、温度、流量、长度、重量、体积换算 |
| `retrieve_specs` | `tools/retrieve.py` | 跨规范文档语义检索 |
| `lookup_section` | `tools/lookup.py` | 按文档提示和章节号精确查询 |
| `search_within_doc` | `tools/lookup.py` | 限定某份规范内语义检索 |
| `query_database` | `tools/sql_tool.py` | NL2SQL 查询 MySQL，并生成 Markdown/图表 |

## 6. Agent 路由

| 路由 | 节点 | 适用场景 |
| --- | --- | --- |
| `qa` | `qa_node` | 规范概念、定义、流程、条款解释 |
| `sql` | `sql_node` | 事务/任务/统计/清单类业务数据 |
| `general` | `general_node` | 单位换算、复合工具调用、兜底 |
| `tools` | `ToolNode` | ReAct 工具执行节点 |

## 7. MCP 接口

当前 MCP Server 使用官方 `mcp` SDK 的 FastMCP 暴露工具。

支持：

- stdio。
- streamable-http。
- 通过 `MCP_ENABLED` 开关切换本地工具和 MCP 工具。

当前 MCP 暴露工具与 LangChain 工具保持同类能力：换算、条款查询、文档检索和规格检索。

## 8. 前端接口调用

前端服务文件：

```text
frontend/src/services/chatStream.js
```

核心行为：

1. `checkHealth()` 调用 `/health`。
2. `streamChat()` 调用 `/api/chat/stream`。
3. `sendChat()` 调用 `/api/chat`。
4. `login()` 调用 `/api/auth/login`。
5. `getMe()` 调用 `/api/auth/me` 解析本地 token。
6. 请求体携带 `session_id` 和 `user_id`。
7. 收到 `meta.session_id` 后保存当前会话。
8. `listSessions()` 调用 `/api/sessions`。
9. `getSession()` 调用 `/api/sessions/{session_id}`。
10. `deleteSession()` 调用 `/api/sessions/{session_id}` 并携带 `user_id`。

## 9. 登录与权限接口

当前已实现基础登录接口。登录优先读取当前数据库中的 `user` 表；若本地开发环境无法连接 MySQL，则允许使用 `admin/admin` 和 `engineer/engineer` 作为非生产演示账号。

用户表字段：

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `user_id` | int | 用户主键 |
| `username` | varchar(255) | 账号名 |
| `password` | varchar(255) | 账号密码 |
| `authority_flag` | int | `0=engineer`，`1=admin` |

已实现 API：

```http
POST /api/auth/login
GET /api/auth/me
```

当前阶段不实现后端 logout，前端清理 localStorage 中的本地 token 即可。

登录成功后应返回用户信息和角色：

```json
{
  "token": "local-demo-token",
  "user": {
    "user_id": "1",
    "username": "admin",
    "role": "admin",
    "authority_flag": 1,
    "permissions": ["用户管理"]
  }
}
```

当前 token 是前端本地演示 token，不是 JWT，也不作为生产级鉴权凭据。

## 10. 评估结果接口

### 10.1 最新 Golden Set 评估摘要

```http
GET /api/evaluation/latest
```

用途：读取本地 `data/eval_results/golden_eval_summary.json`，返回管理员评估看板需要的聚合摘要。该接口不返回原始 Golden Set 问题、不返回完整 SQL、不返回完整检索片段，仅返回数据集画像、合约指标、Prediction 指标和场景分布。

默认评估文件路径可通过环境变量覆盖：

```env
EVAL_RESULTS_PATH=data/eval_results/golden_eval_summary.json
```

如果接口返回 404，说明当前环境还没有生成评估摘要，需要先运行：

```powershell
uv run python scripts/eval_golden_set.py
uv run python scripts/replay_golden_set.py --mode oracle --evaluate
```

响应示例：

```json
{
  "title": "Golden Set 评估摘要",
  "generatedAt": "2026-06-24 15:27",
  "source": "data/eval_results/golden_eval_summary.json",
  "dataset": {
    "dialogues": 100,
    "turns": 390,
    "sqlExpectations": 140,
    "ragEvidence": 110,
    "memoryStates": 390
  },
  "contractMetrics": [
    {
      "label": "SQL 模板通过率",
      "value": "100%",
      "detail": "140 / 140"
    }
  ],
  "predictionMetrics": [
    {
      "label": "RAG Recall@5",
      "value": "100%",
      "detail": "oracle retrieved"
    }
  ],
  "scenarioCounts": [
    {
      "label": "NL2SQL 条件记忆",
      "value": 120
    }
  ]
}
```

前端行为：管理员工作台优先调用该接口；当后端不可用或评估文件不存在时，前端回退到 `frontend/src/data/evaluationSummary.js` 中的静态摘要，保证演示页面可用。

## 11. 后续管理员接口规划

管理员接口规划：

| 能力 | 建议接口 |
| --- | --- |
| 用户管理 | `/api/admin/users` |
| 角色分配 | `/api/admin/users/{user_id}/role` |
| 文档上传/删除/更新 | `/api/admin/documents` |
| 知识库重建 | `/api/admin/knowledge/rebuild` |
| 向量索引管理 | `/api/admin/vector-index` |
| 数据库连接配置 | `/api/admin/database-config` |
| 工具/API 配置 | `/api/admin/tools` |
| Agent 提示词配置 | `/api/admin/prompts` |
| 模型参数配置 | `/api/admin/model-config` |
| 全部问答记录 | `/api/admin/conversations` |
| 全部工具调用记录 | `/api/admin/tool-logs` |
| 系统运行日志 | `/api/admin/system-logs` |
| 质量评估结果 | `/api/admin/evaluations` |
| 数据导出 | `/api/admin/export` |
| 异常数据删除 | `/api/admin/anomaly-data` |
| 工程师反馈审核 | `/api/admin/feedback` |

## 12. 后续工程师接口规划

工程师接口规划：

| 能力 | 建议接口 |
| --- | --- |
| 个人历史问答 | `/api/me/conversations` |
| 回答反馈 | `/api/me/feedback` |
| 标记回答正确性 | `/api/me/answer-ratings` |
| 知识库补充建议 | `/api/me/knowledge-suggestions` |

## 13. 待补充信息

请后续补充：

1. 是否需要正式 OpenAPI 文档导出为 Markdown。
2. MCP streamable-http 的生产端口和鉴权方式。
3. `/api/sessions` 是否需要搜索、重命名和会话 Markdown 导出。
4. 是否需要把 `/api/evaluation/latest` 扩展为可触发评估任务、查看历史评估运行和下载评估报告。
